name: monkeydart
description: Thorsten Ball monkey interpreter written in Dart
version: 0.0.1


environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.1
  cli_completion: ^0.3.0
  equatable: ^2.0.5
  fpdart: ^0.6.0
  mason_logger: ^0.2.5
  meta: ^1.9.1
  pub_updater: ^0.3.0

dev_dependencies:
  build_runner: ^2.4.4
  build_verify: ^3.1.0
  build_version: ^2.1.1
  mocktail: ^0.3.0
  test: ^1.24.2
  very_good_analysis: ^5.0.0

executables:
  mkay:
