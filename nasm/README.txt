INFO:
	we are using the default Linux calling convention for x86_64
	to read more: https://en.wikipedia.org/wiki/X86_calling_conventions#System_V_AMD64_ABI

	At the moment, we are using libc but we plan on removing it at some time.
